-- SQLite
-- Consulta capaz de exibir quantas locações existem na tabela de LOCATIONS

-- Consultando Tabela LOCATIONS (Locações)
SELECT * FROM LOCATIONS;

-- Contando locações
SELECT COUNT(ID) AS 'QUANTIDADE DE LOCAÇÕES' FROM LOCATIONS;